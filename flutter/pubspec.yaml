name: feeddiary
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  riverpod_annotation: ^4.0.2
  go_router: ^17.2.3
  dio: ^5.9.2
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  flutter_secure_storage: ^10.3.1
  shared_preferences: ^2.5.5
  lottie: ^3.3.1
  image_picker: ^1.1.0
  url_launcher: ^6.3.0
dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  riverpod_generator: ^4.0.3
  freezed: ^3.2.6-dev.1
  json_serializable: ^6.14.0
  http_mock_adapter: ^0.6.1
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.8

# 런처 아이콘 생성 — RN 자산 1:1. 1024 마스터(전체 장면)는 iOS·web·legacy Android, RN foreground(레모니)는 Android adaptive(흰 배경).
flutter_launcher_icons:
  image_path: "assets/icon/app_icon.png"
  android: true
  ios: true
  remove_alpha_ios: true
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"
  adaptive_icon_foreground_inset: 0
  web:
    generate: true
    image_path: "assets/icon/app_icon.png"
    background_color: "#FFFFFF"
    theme_color: "#FFFFFF"

# 네이티브 스플래시 — Android 12+ SplashScreen API 정석: 흰 배경 + 레모니(중앙·비번화면 캐릭터 lemony3). 워드마크는 인앱 SplashScreen 이 받는다. RN bootsplash 와 동일 구성.
# android_12 는 OS 가 아이콘을 원형 마스크에 넣어 꽉 찬 이미지가 잘리므로, 여백 둔 정사각(splash_android12)을 따로 써 원 안에 맞춘다.
flutter_native_splash:
  color: "#FFFFFF"
  image: assets/images/home/lemony3.png
  android_12:
    image: assets/icon/splash_android12.png
    icon_background_color: "#FFFFFF"
  web: false

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # 에셋 — RN src/assets 이식(스티커·편지지/핀·화분 캐릭터·물/사랑 Lottie). 디렉토리 단위 등록.
  assets:
    - assets/images/sticker/
    - assets/images/letter/
    - assets/images/home/
    - assets/images/character/
    - assets/images/signin/
    - assets/images/profile/
    - assets/images/diary/
    - assets/lottie/

  # 폰트 — RN assets/fonts 이식. Dovemayo_gothic=본문/UI 기본(단일 weight), Ownglyph=손글씨 강조.
  # 아이콘 폰트(assets/fonts/icons/*)는 RN react-native-vector-icons 와 동일한 .ttf 를 직접 번들한다.
  # Flutter 3.44 에서 IconData 가 final 이 되어 아이콘 패키지의 IconData 상속이 컴파일 불가 → 폰트 직접 번들이 견고·충실.
  # 사용 글리프만 릴리스 빌드의 아이콘 트리셰이킹으로 서브셋된다. 코드포인트 매핑은 ui/core/icons/feed_icons.dart.
  fonts:
    - family: Dovemayo_gothic
      fonts:
        - asset: assets/fonts/Dovemayo_gothic.ttf
    - family: Ownglyph_PDH-Rg
      fonts:
        - asset: assets/fonts/Ownglyph_PDH-Rg.ttf
    - family: FontAwesome5_Solid
      fonts:
        - asset: assets/fonts/icons/FontAwesome5_Solid.ttf
    - family: FontAwesome
      fonts:
        - asset: assets/fonts/icons/FontAwesome.ttf
    - family: Ionicons
      fonts:
        - asset: assets/fonts/icons/Ionicons.ttf
    - family: AntDesign
      fonts:
        - asset: assets/fonts/icons/AntDesign.ttf
    - family: Octicons
      fonts:
        - asset: assets/fonts/icons/Octicons.ttf
    - family: Feather
      fonts:
        - asset: assets/fonts/icons/Feather.ttf
    - family: MaterialCommunityIcons
      fonts:
        - asset: assets/fonts/icons/MaterialCommunityIcons.ttf
    - family: Entypo
      fonts:
        - asset: assets/fonts/icons/Entypo.ttf
