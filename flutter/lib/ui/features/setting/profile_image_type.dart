// 프로필 이미지 종류 — 사진(업로드) vs 캐릭터(프리셋). 가입/수정 컨트롤러와 공유 위젯이 공유. RN ProfileImageSection 의 profileImageType.

/// 프로필 이미지 입력 모드. 사진([photo])과 캐릭터([character])는 상호 배타다(RN ProfileImageSection).
enum ProfileImageType { photo, character }
